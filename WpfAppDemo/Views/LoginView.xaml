<UserControl x:Class="WpfAppDemo.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:local="clr-namespace:WpfAppDemo.Views"
             xmlns:helpers="clr-namespace:WpfAppDemo.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Width="250"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">


        <!-- Label Username -->
        <TextBlock Text="Username:" Margin="0 5 0 0"/>
        <TextBox x:Name="Username"
                 Margin="0 5"/>

        <!-- Label Password -->
        <TextBlock Text="Password:" Margin="0 5 0 0"/>
        <PasswordBox x:Name="PasswordBox"
             Margin="0 5"
             helpers:PasswordBoxHelper.BindPassword="True"
             helpers:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay}" />
        <!--<PasswordBox x:Name="PasswordBox"
                     Margin="0 5"/>-->

        <!--<Button x:Name="Login"
                Content="Login"
                Margin="0 10"
                cal:Message.Attach="[Event Click] = 
                [Action Login(PasswordBox.Password)]"/>-->
        <Button x:Name="Login"
        Content="Login"
        Margin="0 10"/>
    </StackPanel>
</UserControl>
